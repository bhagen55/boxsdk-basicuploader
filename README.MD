To compile (Tested on Windows)
1. Install pipenv
	pip install pipenv
2. Create a new pipenv project
	pipenv --python 3.6
3. Enter pipenv
	pipenv shell
1. Install pyinstaller (pip install pyinstaller)
